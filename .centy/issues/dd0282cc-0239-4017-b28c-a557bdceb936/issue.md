# Missing `.centy` folder

When running \`\`\`centy list issues

› Error: .centy folder not initialized. Run "centy init" first.\`\`\`

Instead of telling the user to init tell him that there is not `.centy` folder in the current cwd so they can or cd to the folder or init
