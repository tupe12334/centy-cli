# Hide uninitialized projects from project list by default

Filter out projects that are not initialized (no .centy manifest) from the project list by default. Add --include-uninitialized flag to optionally include them.
